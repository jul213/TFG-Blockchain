global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'tfg_backend'
    static_configs:
      - targets: ['tfg_backend_final:3000']
